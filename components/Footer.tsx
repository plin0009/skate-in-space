import styles from "../styles/components/Footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>For Hack the North 2020++.</p>
    </footer>
  );
};

export default Footer;
